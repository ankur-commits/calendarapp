---
description: Quick health check of CI, Railway backend, and deployment status.
---
1. Run `gh run list --repo ankur-commits/calendarapp --limit 5` to see recent CI runs.
2. Run `railway status` to check backend service status.
3. Summarize: which CI runs passed/failed, current backend environment, any issues detected.
