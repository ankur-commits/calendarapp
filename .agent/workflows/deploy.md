---
description: Deploy changes to staging by committing and pushing to git.
---
1. Run `git add .` to stage all changes.
// turbo
2. Run `git commit -m "Auto-deploy: Update codebase"` to commit changes.
// turbo
3. Run `git push origin staging` to push to staging and trigger deployment.
